SafeCity Fixed - Ready to run
- Java 17, Maven, MySQL required
- DB: create database safecity manually or let app create (needs CREATE DB privileges).
- MySQL credentials in src/main/resources/application.yml (root/yourpassword)
- Import into Eclipse as Maven project and run SafecityApplication
